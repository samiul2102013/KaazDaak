DJANGO_SETTINGS_MODULE=config.settings.development
SECRET_KEY=change-me-to-a-50-char-random-string 
DEBUG=True
###%eH7e4bz41FCl4$cU

POSTGRES_DB=kaaz_daak_db
POSTGRES_USER=kaaz_daak_user
POSTGRES_PASSWORD=strongpassword
POSTGRES_HOST=db
POSTGRES_PORT=5432

REDIS_URL=redis://redis:6379/0

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

ALLOWED_HOSTS=localhost,127.0.0.1,74.225.251.88

# OTP bypass (development only) - set OTP_BYPASS_ENABLED=True to allow a
# fixed code in place of the emailed one
OTP_BYPASS_ENABLED=False
OTP_BYPASS_CODE=123456

# Rate limiting - master switch and per-scope overrides
# THROTTLE_ENABLED=False disables all scoped throttles (debug/CI only)
# THROTTLE_RATES overrides individual scopes, e.g.:
#   THROTTLE_RATES=login=5/minute,otp_resend=10/hour,password_reset=5/hour
THROTTLE_ENABLED=True
THROTTLE_RATES=
